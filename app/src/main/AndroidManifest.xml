<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.inrating.poststat">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="top.inrating.poststat.ui.PostStatisticsActivity"
            android:theme="@style/PostStatisticsTheme">
        </activity>

        <activity
            android:name="top.inrating.poststat.TestActivity"
            android:theme="@style/PostStatisticsTheme"
            android:windowSoftInputMode="stateVisible|adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name="top.inrating.poststat.DebugActivity"
            android:theme="@style/PostStatisticsTheme">
        </activity>

    </application>
</manifest>
